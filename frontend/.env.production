# Used automatically by `vite build` (production). Points the deployed frontend
# at the live Render backend. Not a secret — the URL is visible in the built app.
VITE_API_URL=https://remittance-comparison-platform.onrender.com
